// import express, mongoose, dotenv

// load .env vars with dotenv

// create Express app

// use express.json middleware

// add GET /api/test route, return "Server running"

// start server on port from .env


// after we've completed code above - 
// connect to MongoDB with mongoose using MONGO_URI

// log connection errors